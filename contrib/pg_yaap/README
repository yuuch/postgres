# Yet Another Analyze Process (YAAP)

YAAP 是 `contrib/pg_yaap` 下的分析型优化器 + 执行器原型。当前主线已经明确：

- **只支持 YAAP 自己的 optimizer physical plan。**
- **不再把 PostgreSQL 的 `PlannedStmt` / executor plan 当成受支持输入。**
- optimizer 与 executor 通过共享的 `PhysicalOperator` / `ColumnBinding` 语义强绑定，但模块职责仍然分离。

## 当前架构

端到端主路径：

```text
PG Query
  -> planner_hook / YaapAdapter
  -> LogicalPlanner
  -> LogicalOptimizer
  -> PhysicalPlanner
  -> OptimizerPlanBundle::physical_plan
  -> yaap_opt_translator
  -> pipeline runtime
  -> OutputSink
```

当前约束：

1. `yaap_opt_translator` 的输入是 **YAAP optimizer 生成的 `PhysicalOperator` 树**，不是 PG plan tree。
2. optimizer path 上不再应把 `varno/attno` 当作权威列标识；权威键是 `ColumnBinding(table_index, column_index)` 与 operator 输出字典。
3. translator 尽量只做 lowering；如果 DuckDB 有对应算子/方案，优先补 operator/runtime，而不是在 translator 里硬改 plan shape。
4. YAAP claim 了 query 之后不应 silent fallback 到 PG 执行。

## 当前进展（TPC-H 10G，2026-05-15）

已在 YAAP optimizer + pipeline 路径上跑通并确认可执行：

- Q1
- Q2
- Q3
- Q4
- Q5
- Q6
- Q7
- Q8
- Q9
- Q10
- Q11
- Q12
- Q14
- Q17
- Q20
- Q21
- Q22

当前未跑通：

- Q13：`optimizer support analysis passed, but lowering returned null`
- Q16：`optimizer support analysis passed, but lowering returned null`
- Q18：`optimizer support analysis passed, but lowering returned null`

备注：

- Q15 / Q19 仍不纳入当前常规 sweep。
- Q16 当前问题已收敛到 **binding-first 输出元数据在多层 join / projection lowering 中还没有稳定按实际输出顺序传递**。

## 模块职责

### Optimizer

- 输入：PostgreSQL `Query`
- 输出：YAAP `PhysicalOperator` tree
- 目标：尽量贴近 DuckDB 的逻辑/物理规划职责划分

当前关键类型：

- `LogicalOperator`
- `Expression`
- `ColumnBinding`
- `PhysicalOperator`
- `PhysicalOperator::OutputColumn`

其中 `ColumnBinding(table_index, column_index)` 是 optimizer path 上的权威列 identity；`OutputColumn` 是 operator 输出侧的字典，用来把 binding、名字和输出顺序稳定带给 lowering。

### Executor

- 输入：optimizer physical plan
- 入口：`yaap_opt_translator`
- 运行时：`parallel/pipeline`

执行器侧要求：

- translator 只做 plan lowering，不承担 optimizer 应承担的 shape 重写。
- runtime 能力不足时，优先补 runtime/operator，而不是写 query-specific translator hack。
- optimizer path 上的列解析应优先依赖 binding dictionary / operator outputs，而不是依赖 PG `varno/attno`。

## 开发与验证

构建：

```bash
meson compile -C build pg_yaap
meson install -C build --only-changed
```

运行单条调试：

```sql
LOAD 'pg_yaap';
SET pg_yaap.enabled = on;
SET pg_yaap.parallel = on;
SET pg_yaap.trace_hooks = on;
SET pg_yaap.trace_execution_path = on;
```

TPC-H 验证约束：

- 单条 query，`statement_timeout = '60s'`
- 失败后继续后续 query
- 修单条 query 时，先只回归当前 query，再做 sweep

## 当前路线

1. 继续把 optimizer-to-executor lowering 全面收敛到 **binding-first metadata**。
2. 清理 translator 中遗留的 PG-plan / `varno/attno` 主键式思路。
3. 修通 Q16，再处理 Q13 / Q18。
4. 在 optimizer physical plan 路径稳定后，再继续补 runtime operator 覆盖面。
